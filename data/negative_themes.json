{
  "comment": "Themes for negative (credential-free) samples. Each string completes the sentence 'Generate a snippet that <theme>'. Add entries freely.",
  "themes": [
    "reads all secrets from environment variables using os.getenv",
    "loads credentials from a config file using a secrets manager",
    "authenticates via an OAuth flow with no hardcoded values",
    "uses a database connection pool with credentials injected from environment variables",
    "makes an API request where the key is fetched at runtime from AWS Secrets Manager",
    "retrieves a token from HashiCorp Vault at startup with no hardcoded fallback",
    "reads a private key path from config and loads it from disk at runtime",
    "uses Kubernetes secrets mounted as environment variables",
    "sets up structured logging and monitoring with no secrets present",
    "defines utility functions for string manipulation and text processing",
    "implements a retry decorator with exponential backoff",
    "defines data models or schema classes with no credentials",
    "implements pagination logic for a REST API client",
    "parses command-line arguments for a CLI tool",
    "sets up a health-check endpoint that returns server status",
    "implements a rate limiter using a token bucket algorithm",
    "defines unit tests for a utility module with mocked dependencies",
    "sets up a CI pipeline config that references secrets via masked variables",
    "configures a linter or formatter with no sensitive values",
    "trains a neural network on a local dataset with no external API calls",
    "loads a pre-trained model checkpoint from disk and runs batch inference",
    "preprocesses a CSV dataset with pandas — filtering, normalisation, train/val split",
    "defines pytest fixtures that stub out an HTTP client with no real credentials",
    "runs a database schema migration using an ORM with no connection string in code",
    "defines a Terraform module for a VPC with no secrets in outputs or variables",
    "writes a Makefile with build, test, and lint targets and no secret values",
    "configures an nginx reverse proxy block with no tokens or passwords",
    "defines React component state and event handlers with no API keys",
    "sets up a GraphQL resolver that delegates auth to a middleware layer",
    "implements a CLI subcommand that reads a config file path from a flag",
    "defines a message queue consumer that reads broker credentials from the environment",
    "sets up distributed tracing with OpenTelemetry and no hardcoded endpoints",
    "implements a cache warming script that uses connection details from a config map",
    "writes a GitHub Actions job that checks out code and runs tests with no secrets in YAML",
    "defines a Helm chart values file with resource limits and replica counts but no credentials",
    "implements a data pipeline step that reads and writes to local files",
    "defines an Ansible task that installs packages with no passwords in the playbook",
    "sets up feature flags using an environment variable toggle with no API tokens",
    "implements a webhook handler that verifies HMAC signatures using a key from the environment"
  ]
}